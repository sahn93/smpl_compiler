main
var a;

function square(a);
{
    return a * a;
};

function pow(a, b);
var pow;
{
    let pow <- 1;
    while b > 0 do
        let pow <- pow * a;
        let b <- b - 1;
    od;
    let b <- b + 1;
    return pow;
    call OutputNum(pow)
};

void function printZero();
{
    call OutputNum(0)
};

{
    let a <- call pow(3, call square(2));
    call printZero();
    call OutputNum(a)
}.